import { RISK_CONFIG } from "@/lib/risk-engine/risk-config";
export default function Methodology(){return <main className="wrap" style={{padding:"40px 0"}}><h1>Wie funktioniert der Risiko-Score?</h1><p>MEME RADAR verwendet reproduzierbare Heuristiken, kein geheimes KI-Urteil. Befunde aus CONTROL, HOLDERS, LIQUIDITY, TOKEN_MECHANICS, MARKET und CREATOR_BEHAVIOR werden anhand dokumentierter Gewichte relativ zu verfügbaren Checks auf 0–100 normiert.</p><div className="grid cards">{RISK_CONFIG.bands.map(b=><article className="panel" key={b.max}><h2>bis {b.max}</h2><p>{b.label}</p></article>)}</div><section className="panel" style={{marginTop:18}}><h2>Schwellenwerte und Gewichte</h2><pre>{JSON.stringify({thresholds:RISK_CONFIG.thresholds,weights:RISK_CONFIG.weights,minimumReliableCoverage:RISK_CONFIG.minimumReliableCoverage},null,2)}</pre></section><section className="panel" style={{marginTop:18}}><h2>Limitierungen</h2><ul><li>Ein niedriger Score garantiert nichts; neue Betrugsmethoden können unbekannt sein.</li><li>Creator-Inferenz kann unsicher sein und wirtschaftlich zusammengehörige Wallets können getrennt erscheinen.</li><li>Marktdaten können verzögert sein oder ausfallen; LP-Lock ist nicht immer eindeutig bestimmbar.</li><li>Transfer-Hooks können komplexe Programmlogik enthalten, die die Erkennung nicht vollständig prüft.</li></ul></section></main>}
